import { redirect } from 'next/navigation';

export default function ResourcesPage() {
  // Redirect to the LinkedIn articles page
  redirect('https://www.linkedin.com/feed/following/'); // Placeholder LinkedIn URL
}